database: postgre
schema: public
table: preferences
columns:
  member_id:
    type: "VARCHAR(100)"
    constraints: ["NOT NULL"]
  preference:
    type: "VARCHAR(255)"
indexes:
  - name: "idx_member_id_pref"
    columns: ["member_id"]
